import React from 'react';
import { Container, BoardTitle } from './headerCss';

const Header = () => {
  return (
    <Container>
      <BoardTitle>Board Title</BoardTitle>
    </Container>
  );
};

export default Header;
