import React from 'react';
import styled from '@emotion/styled';

const Title = styled.h2`
  margin: 0.5rem 1rem;
  font-weight: 500;
  font-size: 16px;
  color: #172b4d;
`;

const ListTitle = () => {
  return <Title>List 1</Title>;
};

export default ListTitle;
